/**
 * Identity lifecycle: SAML single logout, social login, SCIM (via shared registrar).
 */
import type { FastifyInstance } from 'fastify';
export default function provisioningRoutes(fastify: FastifyInstance): Promise<void>;
//# sourceMappingURL=provisioning.routes.d.ts.map