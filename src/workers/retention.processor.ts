/**
 * Retention worker placeholder.
 *
 * No BullMQ processor is implemented here yet. Future retention work should
 * enforce data-retention policy in background batches so cleanup does not block
 * API requests.
 */
